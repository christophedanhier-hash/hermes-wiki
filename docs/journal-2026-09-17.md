# Journal du 2026-09-17 — Wiki Hermes/LEO

> Généré le 17/09/2026 à 23:06 (édition complète)

## 📚 Documentation disponible

Sections actives du wiki Hermes : guide d'installation, profils & gateways, crons, providers LLM, dashboard, skills, intégrations (Drive, Gmail, véhicule, énergie, réseau, **hive inter-profils**) + le guide d'implantation Hive pour plateforme tierce (11/09), le guide Hive v3 (13/09), la skill **`herdr-agent-multiplexer`** (14/09) et la chaîne **DSH** (veille de versions + `/dsh-go/`, 15/09).

⚠️ **À documenter (nuit du 17/09)** : ① le **registre des briques** (`registre-briques.json` — 109 briques, états `active` / `en_pause` / `retiree` / `inconnu`, et la règle « sans intention DÉCLARÉE, décision ≡ panne ») ; ② la **bascule Leo Chat** sur `gpt-5.6-luna` (Azure Foundry a perdu 4 déploiements) ; ③ le nouveau contrat Hive « clôture prouvée » — le **guide Hive v3 décrit encore l'ancien contrat** (`done` = fermeture), obsolète depuis le 16/09 21:30.

## 🧑‍💻 Activité (journée)

- **23 auto-commits horaires `LEO Bot`** (00:15 → 22:15) — aucun commit manuel sur le wiki.
- **4 auto-commits `doc-crons-sync`** sur BAVI_LEO (00:00/06:00/12:00/18:00) — **31e jour vert, 0 conflit UU**.
- **12 commits manuels** sur `my-emile-ia/myxxx-instance` (05:39 → 18:45, auteur `LEO (Christophe's Hermes)`) — import Word/PDF sécurisé, suppression contrôlée, rendu Mermaid (repli sûr puis export HTML), navigation documents longs, responsive print, images sûres + accessibilité.
- **16 commits** sur BAVI_LEO dont **5 manuels** : Azure/Leo Chat, Kiro/OpenRouter, deux correctifs Hive + le correctif « messages traités », et **2 commits Robert** (world models).
- **0 commit** sur `wiki-oca` (7e jour) et `emile-wiki` (20e jour).

## 📝 Note

**🟢 Le registre des briques (matin).** La leçon de l'audit Hive — *un contrat faux produit du silence plausible* — a été appliquée à l'infrastructure : **109 briques inventoriées** avec un état **déclaré** (`active`, `en_pause`, `retiree`, `inconnu`) et une source (`mesure` / `a_confirmer` / `christophe`). **5 nouveaux crons de gouvernance** ont été créés (73 → **79 crons multi-profils**) : santé des crons, expirations d'accès manuels, **écart déclaré ↔ réel**, **dérive des copies de scripts (3 chemins)**, **réconciliation des demandes non suivies**. **Viessmann est déclaré `en_pause`** — décision de Christophe du 17/09 consignée : donnée figée = normal, plus d'alerte. **4 briques restent `inconnu`** (app My Émile IA inactive, benchmark-providers figé 47 j, health-state.json figé 54 j, Eneco/borne sans brique active) : elles attendent un arbitrage humain.

**🟠 Azure Foundry a perdu 4 déploiements.** `DeepSeek-V4-Flash`, `DeepSeek-V4-Pro`, `gpt-4o` et `gpt-4o-mini` renvoient `DeploymentNotFound` sur `ia-solidaris-cd-resource` ; seuls **terra** et **luna** répondent. Leo Chat chargeait le modèle mort par défaut → **choix A de Christophe** (rester sur Solidaris) : défaut `gpt-5.6-luna`, whitelist azure réduite aux 2 vivants, cache-bust v14, **envoi réel vérifié sur tofdan.be** (commit `52f0627f`).

**🟠 OpenRouter réparé — 350 modèles ressuscités.** Tous les modèles OpenRouter échouaient en 401 : `server.py` lisait `OPENROUTER_KEY` alors que la variable est `OPENROUTER_API_KEY` — **un mot** (`3b2a1f66`). **Ollama rendu dynamique** : sa liste était écrite en dur (`qwen3:8b`, `qwen2.5:7b`) alors que Gemini et OpenRouter étaient déjà dynamiques — un modèle ajouté dans Ollama (MiniCPM5-2B) restait invisible. Vérifié sur localhost **et** tofdan.be.

**🐝 Hive — deux angles morts fermés.** ① **Régression de fuseau** (introduite la veille par une correction erronée) : l'âge d'un message devenait **négatif**, le réveil informatif ne partait qu'à 150 min au lieu de 30 — trouvée par l'**audit externe DSH**, corrigée à 08:28. ② **« Le problème de boîte »** : le listing par défaut ne montrait que `inbox/`, or un message traité est déplacé dans `inbox/.done/` — **155 messages invisibles pour Émile seul** ; d'où « il ne s'affichait pas dans ma boîte Hive habituelle ». Correctif `hive-inbox.py --recents` (17:48). Le nouveau contrat « clôture prouvée » est utilisé en réel : **2 `declaree_faite`, 1 `partial`, 1 `verify`**.

## À suivre

- [ ] **Documenter le registre des briques** et le **nouveau contrat Hive** dans le wiki (guide v3 obsolète) ; documenter la bascule Azure (luna/terra) et le correctif OpenRouter (`OPENROUTER_API_KEY`).
- [ ] **Créer** la page « États des briques » (les 4 `inconnu` attendent une décision) — **et le rappel des 5 nouveaux crons** dans la page crons.
- [ ] **`wiki-oca` dirty** : 23 fichiers non suivis (journaux 28/08 → 16/09, scripts Atlas) — à committer, sinon le wiki publié est amputé.
- [ ] `📊 Unified Collector v2` : **17/17 sources OK**, `alerts: []`, dossier #5 clos ; `💰 Budget Check v6` **38e jour sans 404** ; `📋 Frontmatter` ✅. Écosystème : **79 crons** (michel 68 + leo 8 + sylvia 2 + emile 1) — **76 actifs, 0 erreur, 0 pending, 3 désactivés** — **journée 100 % verte** après l'incident Cariad/VW de la veille (Skoda repassé au vert dès 00:23).
