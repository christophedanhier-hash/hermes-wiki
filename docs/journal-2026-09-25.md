# Journal — vendredi 25 septembre 2026

## Documentation disponible

| Section | Contenu |
|---|---|
| `hermes/` | Architecture, gateways, providers, profils Hermes |
| `AUDIT.md` | Audit de conformité de la documentation |
| `BAVI_ONTOLOGY.md` | Ontologie de l'écosystème BAVI |
| `archives/` | Journaux et documents archivés |
| `journal-YYYY-MM-DD.md` | Journaux quotidiens |

## Activité

- **Dernier commit** : `1153a0e` — 25/09 22:15, `chore: auto-commit modifications` (LEO Bot).
- **Commits du jour** : **23** — **23 auto-commits** LEO Bot (cadence horaire **00:15 → 22:15**), **aucun commit manuel**.
- **État du dépôt** : **propre** (`git status` = 0 fichier modifié) — le wiki ne subit pas la dette d'hygiène de `wiki-oca` / `emile-wiki` / `voyages-wiki`.
- **Note du jour** : journée de **gouvernance et d'auto-correction**. ⚠️ **Incident `reasoning_effort` — erreur infra assumée et corrigée** : les 4 profils DeepSeek (default, emile, gerard, robert) ont été alignés à tort sur `medium` à **18:52** (`medium` prise pour valeur de référence, alors que **`false`** était la valeur posée les 23-24/09 ; le script de vérification fut bloqué par le parseur, l'échec restant invisible) — **restauration à `false` à 20:28**, **état vérifié par le cœur** (`load_config` + `resolve_reasoning_config`). Final : default/emile/robert/gerard `false`, **michel `medium`**, sylvia `minimal` (hors périmètre).
- **Nettoyage de cron** : `📧 Email Unifié (classifieur + check)`, signalé **STALE** (dernier run 23/09 08:30), est **désactivé** ; la fonction est **restaurée** par `📨 Check Gmail — emails importants (leo + christophe)` (*/30). Commit `eb38b2e4` 20:34.
- **Correctif Hive** : commit `d0fa3614` 15:52 (auteur Michel) — « fix(hive): transmettre `engagement_id` à `enregistrer()` + libellé honnête » ; 7 sauvegardes horodatées de `engagements.json` (15:46 → 15:48).
- **Gouvernance Hive** : **51 messages** (15:46 → 20:34) — **attestations runtime obligatoires « conformité LLM Wiki »** (6 messages, 15:46-15:48), **`Switch Michel vers GPT-6 Luna` refusé** (sans objet : supplanté par DeepSeek-V4.1-Flash le 24/09), **consultation infra LEO** sur `reasoning_effort`, restitution d'erreur, clôture STALE. Vérifications tierces LEO rendues **« CONFORME »** à 18:29.
- **Infrastructure** : **88 crons multi-profils** (michel 76 + leo 8 + sylvia 2 + emile 2), **83 actifs, 0 erreur, 0 pending**, 5 désactivés — **6e journée 100 % verte**. Collecteur unifié `complete` **17/17**, `alerts: []`. Uptime **445 h**, pas de reboot. Budget **10,55 $** (−0,29 $). **Gateway michel : 2e jour sans récidive** de la boucle d'auto-restart (`NRestarts=0`).
- **Chantier applicatif** : `ma-bibliotheque` — **20 commits** (08:00 → 15:23) : authentification Google OIDC, préfixe `/bibliotheque` du proxy, cloisonnement par lecteur, conversions Kindle EPUB→AZW3, guide utilisateur, lots Copilot 6 (verrou inter-processus) et 7 (cloisonnement Drive).

## À suivre

- [ ] ⚠️ **Re-vérifier `reasoning_effort` par le cœur** (pas par lecture du YAML) au prochain audit : `false` attendu sur default/emile/robert/gerard, `medium` sur michel.
- [ ] 📄 Corriger les **chiffres de crons périmés** de `hermes/configuration/providers.md` et la ligne « 70 crons », et **rafraîchir la baseline de l'audit rédactionnel** (figée à « 58 crons actifs » contre 83 réels) — 2 anomalies HAUTES persistent sur `BAVI_LEO/docs/index.md`.
- [ ] 📄 **Trancher le cas OpenRouter** : `hermes/architecture.md` (5 lignes : 57, 125, 136, 225, 325) décrit OpenRouter comme provider **actif** du profil sylvia sans rappeler que ce n'est pas le fallback plateforme — décision en attente.
- [ ] Documenter la **résolution de la boucle gateway michel** dans `hermes/architecture.md` §10 (2e jour vert après clôture du 24/09).
- [ ] Formaliser la **rétention du `state.db`** racine (**5,0 Go** — stable depuis le 24/09).
- [ ] Surveiller la **veille DSH** (`0.1.5-rc.3` signalée ; dernier export de sessions **15/09**, soit 10 jours).
